# 5co-CI-kit _RETIRED.txt — 正典から退役したファイルの一覧（層3a・自動隔離の対象）
#
# 書式: 1行1ファイル。kit ルートからの相対パス（例: ci-format-v3.1.css）。
#       # 始まりの行・空行は無視。行内 # 以降もコメント扱い。
#
# 動作: scripts/sync-ci-kit-drive.sh が同期時に、このリストのファイルを各配布先で
#       _archive/retired-YYYYMMDD/ へ「隔離」する（削除しない・180日後に期限削除）。
#       正本に現存するファイルを誤って列挙した場合は安全側でスキップされ警告が出る。
#
# 運用: 退役の追加は必ず template リポへの PR で行う（配布先でのローカル追記は
#       同期で上書きされるため無効）。旧版 CSS は「過去デッキの再現用に残置」が
#       現行規定（V3.2_FORMAT.md）のため、載せる場合は規定の改定とセットで。
#
# 現在の退役ファイル: なし
